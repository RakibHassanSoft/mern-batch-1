# The URL where your Node.js backend is running.
# The frontend reads this in lib/api.ts as process.env.NEXT_PUBLIC_API_URL.
# Anything starting with NEXT_PUBLIC_ is available in the browser.
NEXT_PUBLIC_API_URL=http://localhost:5000
